About
=====

This directory contains simple example programs that show how
to use the primesieve library to generate primes.

Examples build instructions
===========================

Open a terminal or Visual Studio Command Prompt and `cd' into
the primesieve directory. Then run the commands below:

Unix-like OSes
--------------

$ ./configure --enable-examples
$ make

Microsoft Visual C++
--------------------

> nmake -f Makefile.msvc
> nmake -f Makefile.msvc examples
